defaults:
  - profile_salvalai
  - _self_

# Middle 15s smoke window for fast exact-calculation inference optimization scouting.
# Use this before longer smoke or full-song SALVALAI runs; promote only stable,
# token-equivalent, meaningfully faster changes.
start_time: 71000
end_time: 86000
seed: 12345
attn_implementation: sdpa
use_server: false
profile_record_token_ids: true
output_path: "${oc.env:MAPPERATORINATOR_PROFILE_OUTPUT,./logs/profile_salvalai_smoke15}"
